Rust dependency licenses for imgread
Generated by cargo-about from Cargo.lock for the supported release targets.

{{#each licenses}}
================================================================================
{{name}} ({{id}})
Used by:
{{#each used_by}}
  {{crate.name}} {{crate.version}} — {{crate.repository}}
{{/each}}

{{{text}}}

{{/each}}
